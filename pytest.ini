[pytest]
# В репозитории есть `test_automation.py` — это ручной диагностический скрипт
# для проверки реальных интеграций (AmoCRM/AssemblyAI/OpenAI/Gemini) и требует секретов.
# Чтобы `pytest` гонял только unit-тесты без внешних зависимостей, ограничиваем testpaths.
testpaths = tests

